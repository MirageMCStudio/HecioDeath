options:
  bind-matcher:
    all: ["example::*"]


inventory-penalty-example:
  type: "inventory"
  executor:
    filter:
      - "slot::vanilla::0..7"
      - "union"
      - "slot::vanilla::9..17"
      - "filter::amount::percentage::80%"
    drop-executor:
      name: vanilla
      args:
        example: 1.0



level-penalty-example:
  type: "level"
  executor:
    mode: "drop"
    amount: "percentage"
    didnt: 0
    value: "50%"

respawn-penalty-example:
  type: "respawn"
  executor:
    auto: true
    mode: "death"
    location:
      world: "world"
      x: 100
      y: 100
      z: 100
      yaw: 90
      pitch: 0

javascript-penalty-example:
  type: "javascript"
  run: |
    if (typeof event !== 'undefined') {
      event.entity.player.sendMessage("penalty?")
    }
    player.sendMessage("penalty!")